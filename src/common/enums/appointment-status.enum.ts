export enum AppointmentStatus {
  Pending = 'pending',
  Confirmed = 'confirmed',
  EnRoute = 'en-route',
  InProgress = 'in-progress',
  Collected = 'collected',
  Completed = 'completed',
  Cancelled = 'cancelled',
}
